{
  "name": "acgov-sanity-cms-demo",
  "version": "0.0.1",
  "description": "",
  "engines": {
    "node": "^14.15.*"
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "site-dev": "(cd _site) && npm run dev",
    "build-studio": "lerna bootstrap && (cd _studio && npm run build)",
    "build-site": "lerna bootstrap && (cd _studio && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN npm run graphql-deploy) && (cd _site && npm run generate)",
    "graphql-deploy": "lerna run graphql-deploy",
    "postinstall": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ITD-DSS/designer-breakdown-template/issues"
  },
  "homepage": "https://github.com/ITD-DSS/designer-breakdown-template#readme",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.12.8",
    "@sanity/cli": "^2.0.*",
    "dotenv": "^8.2.0",
    "lerna": "^3.22.*",
    "ssh2-sftp-client": "^5.3.1"
  }
}
